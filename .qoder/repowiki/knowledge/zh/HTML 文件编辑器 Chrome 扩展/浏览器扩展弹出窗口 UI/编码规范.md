- 通过 DOM ID（btn-toggle、btn-save、dot、status-text、file-access-warning）在 JS 中直接引用元素，而非 class 选择器。
- 与 content script 的通信统一通过 chrome.tabs.sendMessage 发送 JSON 消息对象 {type: ...}，并在回调中检查 chrome.runtime.lastError 处理连接失败。
- 状态反馈通过切换 .status-dot 的 active/inactive 类并结合 statusText.textContent 文案变化来呈现。
- 当非 file:// 页面或通信失败时，将按钮置为 disabled 并隐藏/显示警告区域，形成一致的错误降级路径。