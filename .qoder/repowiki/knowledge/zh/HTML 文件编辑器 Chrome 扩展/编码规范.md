- 各脚本采用 IIFE + 'use strict' 自包含执行，避免全局污染
- 注入页面的 UI 元素统一添加 data-he 前缀属性（toolbar/handle/toast/hint），以便清理时精准定位
- 跨进程通信统一通过 chrome.runtime.onMessage 并以 msg.type 分派处理函数
- 保存流程优先尝试 File System Access API 覆盖原文件，失败后降级为 Blob 下载